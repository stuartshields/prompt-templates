<!-- Last updated: 2026-03-23 -->
<!--
Template: Build / Implement
When to use: You want code written. New feature, refactor, migration, or any task
where the agent should produce working code with clear acceptance criteria.
For bug fixes, use the Debug / Fix template instead.
Sections:
  - Context: what exists, what this connects to, why it's needed
  - Requirements: numbered list of what must be done
  - Edge cases: error scenarios and boundary conditions to handle
  - Done when: success criteria — what "done" looks like
  - Constraints: scope limits, patterns to follow, things to avoid
  - Verification: how to confirm it works
  - Examples: optional, include when output format matters
-->

Implement [what] in [where].

Context
[One paragraph on what exists, what this connects to, why it's needed]

Requirements
1. [Must do this]
2. [Must do this]
3. [Must do this]

Edge cases
- [Error scenario or boundary condition to handle, e.g. empty input, auth failure, network timeout]

Done when
- [Success criterion 1 — observable, testable outcome]
- [Success criterion 2]

Constraints
- Only modify files in [scope]. [Why — e.g. other areas have a pending refactor]
- Match existing patterns in [reference file]
- Do not [specific thing to avoid — e.g. do not add new dependencies, do not change the public API]
- If anything is unclear, stop and ask rather than guessing.

Verification
- Run [command] and confirm [expected output]
- [Any other checks]

Examples (optional)
[1-3 examples of expected input/output when the format matters. Remove this section if not needed.]
